<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.LaunchModeDemo">

        <!-- 主界面：作为入口，使用默认 standard 模式 -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 1. standard：标准模式，每次启动都创建新实例 -->
        <activity
            android:name=".StandardActivity"
            android:launchMode="standard"
            android:exported="false" />

        <!-- 2. singleTop：栈顶复用模式，栈顶已存在实例则复用并回调 onNewIntent -->
        <activity
            android:name=".SingleTopActivity"
            android:launchMode="singleTop"
            android:exported="false" />

        <!-- 3. singleTask：栈内复用模式，栈内存在实例则复用并清除其上方 Activity -->
        <activity
            android:name=".SingleTaskActivity"
            android:launchMode="singleTask"
            android:exported="false" />

        <!-- 4. singleInstance：单实例模式，独占一个全新任务栈 -->
        <activity
            android:name=".SingleInstanceActivity"
            android:launchMode="singleInstance"
            android:exported="false" />

    </application>

</manifest>
